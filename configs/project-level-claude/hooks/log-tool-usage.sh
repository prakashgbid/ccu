#!/bin/bash
# Pre-tool usage logging hook
# This prevents "no such file" errors in Claude Code

# Optional: Log to a file if needed
# echo "[$(date)] Tool usage: $*" >> ~/.claude/logs/tool-usage.log

# Exit successfully
exit 0