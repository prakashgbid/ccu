#!/bin/bash
# Post-tool result logging hook
# This prevents "no such file" errors in Claude Code

# Optional: Log to a file if needed
# echo "[$(date)] Tool result: $*" >> ~/.claude/logs/tool-results.log

# Exit successfully
exit 0